# Essential third-party packages used by HandE_analysis (direct imports).
# Versions match the working `learner` conda environment.
# Python >= 3.10 recommended (tested with 3.13).

numpy>=1.26
pandas>=2.0
matplotlib>=3.8
seaborn>=0.13
scipy>=1.11
scikit-learn>=1.3
anndata>=0.10
scanpy>=1.10
squidpy>=1.6
tqdm>=4.66